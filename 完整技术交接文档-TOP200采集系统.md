# 抖店商品 TOP200 采集系统：完整技术交接文档

> 文档版本：1.0  
> 更新时间：2026-08-07  
> 项目目录：`D:\前端\ai\Script All\Tiktok TOP200`  
> 当前状态：已实现、已通过自动测试和真实页面完整 TOP200 验收  
> 适用对象：后续开发、测试、运维人员或继续接手的 AI

本文档描述当前已经运行的代码，不是待开发方案。后续接手者应先读本文档，再按需阅读 [原始需求与技术基线](./技术交接文档-TOP200采集系统.md)、[抖店环境与采集约束](./抖店环境与采集约束.md) 和 [用户使用说明](./使用说明.md)。

---

## 1. 一页交接摘要

这是一个只在本机运行的 Node.js 网页工具。用户通过本地页面选择行业类目、品牌类型和榜单，程序连接真实 Microsoft Edge 中已经登录的抖店会话，逐页检查平台原始前 200 名，只保存“上升”和“首次上榜”的商品，最后生成 Excel 和 JSON。

接手时必须记住以下红线：

1. 登录入口必须使用与同级“商机中心”程序一致的地址：`https://fxg.jinritemai.com/ffa/bu/NewBusinessCenter`。
2. 登录由用户在真实 Edge 中手动完成，禁止导入、复制、解密或迁移 Cookie。
3. 登录完成后，程序才自动进入 TOP200 目标页：`https://compass.jinritemai.com/shop/chance/merchandise-product-rank`。
4. 只处理页面前六个业务榜单，不采集“实时爆品挖掘榜、退后购买榜、同行低退榜”。
5. 每个“类目 × 榜单”只检查平台原始第 1 至第 200 名，不能为了凑满结果继续往后找。
6. 只保存红色上升和“首次上榜”；下降、持平、横线和空趋势只计入排除数。
7. 商品链接必须采集并写入 Excel 可点击超链接；店铺入口是二维码，只记录店铺名称，不采集店铺链接。
8. Excel 中的“当前排名”是平台原始排名，不能把筛选后的结果重新编号。
9. 页面卡顿时有限重试，不刷新采集页面，不高频点击，不无限重试。
10. 同级 `商机中心` 只读参考，不能修改，也不能把本项目文件写入其目录。

最常用命令：

```powershell
npm install
npm start
npm run check
```

普通用户使用两个入口：

- `安装依赖.bat`：每台电脑首次运行一次。
- `启动采集.bat`：日常启动；运行期间黑色窗口必须保持打开。

---

## 2. 已确认的业务口径

### 2.1 用户输入

- 行业类目：从真实页面动态读取，支持搜索和多选，保存完整层级路径。
- 品牌类型：只允许“不限、知名品牌、非知名品牌”三选一。
- 榜单类型：动态读取页面前六个标签，支持多选。
- 时间周期：用户不手选，程序自动决定。

### 2.2 支持的榜单

当前真实页面前六榜为：

1. 总榜
2. 搜索榜
3. 直播榜
4. 商品卡榜
5. 达人带货榜
6. 短视频榜

代码以“前六个标签”为边界动态读取，不依赖这六个中文名称写死。后三个特殊榜必须排除。

### 2.3 周期规则

1. 当前榜单提供“实时”时选择实时。
2. 没有实时时选择“近1天”。
3. 两者都没有时，该组合失败并记录原因。
4. 每条结果同时保存 `requestedPeriod=实时` 与真实的 `actualPeriod`。
5. 不采集近7天或近30天。

真实页面验证结果：总榜、商品卡榜、短视频榜支持实时；搜索榜、直播榜、达人带货榜回退近1天。页面以后可能变化，程序每次仍以当前可见单选项为准。

### 2.4 TOP200 与趋势过滤

处理顺序必须是：

```text
读取平台原始排名 1..200
  -> 识别每条趋势
  -> 保存 up / first
  -> 排除 down / equal / none
  -> unparseable 写失败记录，不进入结果
```

趋势结构：

| 页面状态 | `trendType` | 是否进入明细 | `riseCount` |
|---|---|---:|---|
| 红色上升箭头，如 `↑ 12` | `up` | 是 | `12` |
| 首次上榜 | `first` | 是 | `null` |
| 下降 | `down` | 否 | `null` |
| 持平 | `equal` | 否 | `null` |
| 横线、空趋势 | `none` | 否 | `null` |
| 无法识别 | `unparseable` | 否 | `null`，同时记失败 |

### 2.5 商品与店铺链接

- 优先读取商品标题或其父级链接中的真实 `href`；没有 `href` 时，最多单击、双击商品标题各一次并捕获新页面 URL。
- 二维码入口（如 `ecom-popover-open`）明确跳过，不点击、不解码、不保存。
- 存在标题链接入口但两次交互仍失败时，保留业务记录、令 `productUrl` 为空并写入“商品链接”失败；只有二维码而无真实入口时保留空链接但不制造失败记录。
- 店铺列的入口是二维码，不是可回查的普通店铺链接；只保存 `shopName`。
- 不保存商品图片、图片地址、店铺二维码或店铺链接。

---

## 3. 运行环境与依赖

### 3.1 必需环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows |
| Node.js | 20 或更高版本 |
| 浏览器 | Microsoft Edge |
| 本地网页 | `http://127.0.0.1:8080`，服务只监听本机回环地址 |
| Edge CDP | `http://127.0.0.1:9222` |

### 3.2 npm 依赖

| 包 | 当前约束 | 用途 |
|---|---|---|
| `playwright-core` | `1.58.2` | 通过 CDP 控制用户真实 Edge，不下载 Chromium |
| `exceljs` | `^4.4.0` | 生成包含超链接的 `.xlsx` |
| `uuid` override | `^11.1.1` | 消除依赖树中的已知安全问题 |

最后一次 `npm audit --json` 结果为 0 个已知漏洞。

### 3.3 环境变量

| 变量 | 默认值 | 作用 |
|---|---|---|
| `PORT` | `8080` | 本地服务端口 |
| `CDP_URL` | `http://127.0.0.1:9222` | 连接调试版 Edge 的地址 |
| `NO_OPEN` | 未设置 | 设为 `1` 时服务启动后不自动打开本地网页，主要用于测试 |

---

## 4. 项目目录与文件职责

```text
Tiktok TOP200/
├─ server.js                    本地 HTTP 服务、API、SSE、任务控制
├─ edge.js                      Edge 查找、登录入口、CDP 启动
├─ collector.js                 页面发现、DOM 解析、翻页与采集主流程
├─ collector-utils.js           趋势、周期、字段映射、去重、通用重试
├─ task-store.js                任务、检查点、目录与原子 JSON 写入
├─ export-excel.js              JSON/Excel 报告生成
├─ public/index.html            单文件本地操作网页
├─ test/                        自动测试与脱敏 DOM fixture
├─ data/tasks/                  完整任务状态 JSON
├─ data/checkpoints/            未完成任务检查点
├─ reports/                     正式 JSON 与 Excel 报告
├─ logs/                        日志目录预留
├─ package.json                 依赖、Node 版本、脚本
├─ 安装依赖.bat                 Windows 首次安装入口
├─ 启动采集.bat                 Windows 日常启动入口
├─ 使用说明.md                  面向普通用户
├─ 抖店环境与采集约束.md        浏览器、登录、风控和数据红线
├─ 技术交接文档-TOP200采集系统.md 原始需求与方案基线
└─ 完整技术交接文档-TOP200采集系统.md 本文档，当前实现总入口
```

`node_modules` 可以在新电脑上通过 `npm install` 重建，不应依赖复制旧电脑的依赖目录。

### 4.1 模块接口速查

| 文件 | 导出或入口 | 职责 |
|---|---|---|
| `edge.js` | `CDP_URL`、`LOGIN_URL`、`TARGET_URL` | 浏览器地址常量 |
| `edge.js` | `findEdgePath()` | 按已保存路径、注册表和常见目录查找 Edge |
| `edge.js` | `saveEdgePath(path)` | 验证并保存 `msedge.exe` 路径 |
| `edge.js` | `isCdpReady()` | 检查9222的 `/json/version` |
| `edge.js` | `ensureEdgeDebug()` | 复用或启动调试版 Edge，返回 `ready/launched/need-path` |
| `collector.js` | `connectTargetPage()` | 连接 CDP、处理登录和 TOP200 跳转 |
| `collector.js` | `discoverOptions(log)` | 读取类目、榜单、周期和品牌 |
| `collector.js` | `getTableState(page)` | 提取当前表头、行、页码、指纹和总数 |
| `collector.js` | `waitForStableTable(page, wait)` | 等待连续稳定表格快照 |
| `collector.js` | `runTask(task, control, log, options)` | 执行任务、重试、保存和导出 |
| `collector.js` | `StopRequestedError` | 区分用户停止与程序故障 |
| `collector-utils.js` | `parseTrend()` | 把趋势快照映射为结构化趋势 |
| `collector-utils.js` | `choosePeriod()` | 实时优先，否则近1天 |
| `collector-utils.js` | `mapBusinessFields()` | 映射固定列并保留动态列 |
| `collector-utils.js` | `productKey()` | 生成组合内商品去重键 |
| `collector-utils.js` | `hasPageAdvanced()` | 同时验证页码和页面指纹 |
| `collector-utils.js` | `runWithRetries()` | 通用有限重试执行器 |
| `task-store.js` | `createTask()`、`saveTask()`、`loadTask()`、`listTasks()` | 任务与检查点持久化 |
| `task-store.js` | `atomicWriteJson()` | 临时文件加 rename 的原子JSON写入 |
| `export-excel.js` | `buildSummaryRows()`、`buildDetailRows()`、`buildFailureRows()` | 构建三张工作表数据 |
| `export-excel.js` | `exportTask()` | 生成正式JSON和Excel |
| `server.js` | 直接运行或 `server` 导出 | 启动本地服务与全部API |

其余函数为模块内部实现。新增外部调用前，应先明确是否真的需要扩大公共接口。

---

## 5. 总体架构与数据流

```text
用户浏览器
  | 访问 127.0.0.1:8080
  v
server.js
  |-- 静态网页 public/index.html
  |-- REST API：发现、任务、暂停、恢复、停止、重试、下载
  |-- SSE：实时日志与状态
  |
  +--> edge.js
  |      |-- 查找 msedge.exe
  |      |-- 9222 + 独立 user-data-dir 启动真实 Edge
  |      `-- 先打开商机中心登录入口
  |
  +--> collector.js
  |      |-- connectOverCDP
  |      |-- 登录后自动打开 TOP200
  |      |-- 动态发现类目、榜单、周期、表头
  |      |-- 逐组合、逐页采集
  |      `-- 趋势过滤、商品链接、有限重试
  |
  +--> task-store.js
  |      `-- data/tasks + data/checkpoints
  |
  `--> export-excel.js
         `-- reports/*.json + reports/*.xlsx
```

单进程只允许一个活动任务。一个任务可以包含多个“行业类目 × 榜单”组合，组合按创建顺序串行运行，避免并发操作同一个抖店页面。

### 5.1 关键设计决策与取舍

| 决策 | 选择原因 | 明确代价 |
|---|---|---|
| 真实 Edge + CDP | 保留真实设备和人工登录流程，降低风控与登录失效风险 | 不能完全无人值守，用户每天可能需要登录 |
| 商机中心登录入口与TOP200目标页分离 | 复用已验证登录链路，登录后才进入业务页 | 交互多一次用户确认 |
| 组合串行 | 避免多个自动化流程争抢同一页面状态 | 多类目、多榜单任务耗时更长 |
| 动态发现类目、榜单和表头 | 适配账号权限与页面可见项差异 | 任务开始前发现阶段需要额外时间 |
| 固定等待加DOM验证 | 抖店SPA存在旧DOM和伪翻页，仅等网络事件不可靠 | 单页采集速度低于激进点击方案 |
| 整组从第1页重试 | 不把失败页之前的半成品混入正式结果 | 靠后页面失败时会重复读取前面页面 |
| 原子JSON文件而非数据库 | 便于复制、查看、恢复和跨电脑迁移 | 不适合多进程并发写入或超大规模任务 |
| 单文件前端 | 无需构建工具和外部CDN，普通用户可直接运行 | 页面代码集中，复杂功能继续增长时维护成本会上升 |
| 保存平台区间原文 | 避免把区间中位数伪装成精确值 | 后续数值分析需要单独清洗 |
| 店铺仅保存名称 | 页面入口实际为二维码，无法得到可信普通链接 | 报告不能直接点击进入店铺 |

---

## 6. Edge、登录与页面跳转

### 6.1 两个地址不能混用

```text
登录入口 LOGIN_URL
https://fxg.jinritemai.com/ffa/bu/NewBusinessCenter

采集目标 TARGET_URL
https://compass.jinritemai.com/shop/chance/merchandise-product-rank
```

`edge.js` 启动 Edge 时必须传 `LOGIN_URL`。`collector.js` 在用户完成登录后才打开 `TARGET_URL`。

### 6.2 正确流程

1. 用户点击“读取 / 刷新类目和榜单”。
2. `server.js` 调用 `edge.ensureEdgeDebug()`。
3. 9222 未就绪时，程序查找 Edge 并用以下核心参数启动：

   ```text
   --remote-debugging-port=9222
   --user-data-dir=%TEMP%\edge-debug-top200
   LOGIN_URL
   ```

4. 网页显示登录弹窗，用户在商机中心入口手动登录并完成验证。
5. 用户点击“我已登录，自动进入 TOP200”。
6. `collector.connectTargetPage()` 连接 CDP，创建或复用 TOP200 页。
7. 如果目标页仍被重定向为未登录状态，程序主动回到 `LOGIN_URL` 并返回 `LOGIN_REQUIRED`。
8. 登录有效时读取类目和榜单。

### 6.3 Edge 查找顺序

1. 项目根目录 `.edge-path.txt` 中用户以前保存的路径。
2. Windows 注册表 `App Paths\msedge.exe`。
3. `Program Files (x86)`、`Program Files` 和 `LOCALAPPDATA` 常见位置。
4. 仍找不到时，网页要求用户输入 `msedge.exe` 完整路径，并由 `saveEdgePath()` 验证文件名后保存。

### 6.4 安全边界

- 不导入 Cookie 到 Playwright。
- 不复制默认 Edge 用户目录。
- 不解密 Cookie 或绕过验证码。
- 不使用 Playwright 自带 Chromium 登录。
- `browser.close()` 只断开 CDP 客户端，不负责替用户绕过或管理登录验证。

---

## 7. 真实页面结构与选择器策略

以下信息来自 2026-08-05 的真实页面分析，页面改版后必须重新验证。

### 7.1 组件兼容

页面同时出现两套组件类名前缀：

- `.aurora-*`
- `.ecom-*`

表格、级联选择器和分页选择器都同时兼容两种前缀。不要删除其中一套，除非新 DOM 证据和 fixture 已证明不再需要。

### 7.2 行业类目

- 页面使用多列 Cascader 级联菜单。
- `discoverCategories()` 深度遍历可见菜单，保存 `displayPath` 与可再次点击的 `segments`。
- “全部”作为当前路径的可选节点处理。
- 相同完整路径去重，最终按中文排序。
- 遍历节点安全上限为 5000；最近实测发现 817 个可选类目。

### 7.3 榜单和品牌

- 榜单通过 `[role="tab"]` 获取，只读取前六个。
- 发现阶段逐个点击榜单并读取可用周期，最后恢复原活动标签。
- 品牌筛选通过 `label[title="品牌类型"]` 定位所属表单项，再精确匹配三个标签文本。

### 7.4 表格与排名

- 每页通常 10 行，完整 TOP200 通常为 20 页。
- 只处理可见表格，跳过测量行和列数不完整的行。
- 前三名可能使用奖牌图片而不显示普通数字；此时以 `(页码 - 1) × 当前页行数 + 页内序号` 推导原始名次。
- 页码来自 `.aurora-pagination-item-active` 或 `.ecom-pagination-item-active`。
- 页面指纹由排名、商品名称和部分业务单元格组成，用于判断数据是否真正换页。

### 7.5 趋势 DOM

- 首次上榜：文本精确包含“首次上榜”。
- 上升：类名包含 `upIdentify`，或趋势值类名包含 `up-`。
- 下降：类名包含 `downIdentify` 或 `down-`。
- 持平：类名包含 `equal` 或文本包含“持平”。
- 上升数字从趋势值或趋势文本中的整数解析，支持一位和多位数。

### 7.6 商品和店铺

- 商品名称优先从包含 `name` 或 `title` 的类名中取最长非空文本。
- 价格带从商品单元格中的“价格带”后方文本解析。
- 店铺只提取名称文本。
- 商品链接优先读取标题 `href`；否则通过标题单击、双击并比较点击前后的 `context.pages()` 捕获新页 URL。二维码图标不参与链接获取。

---

## 8. 发现与采集流程

### 8.1 选项发现

`POST /api/discover` 执行：

1. 检查或启动调试版 Edge。
2. 需要新登录时返回 `needLogin=true`，不立刻采集。
3. 登录完成后连接 TOP200。
4. 读取完整类目树。
5. 读取前六个榜单及其周期。
6. 返回三个品牌类型。
7. 将结果保存在服务进程的 `discoveryCache` 中，后续创建任务时用于校验用户输入。

服务重启后必须重新执行发现，因为 `discoveryCache` 只保存在内存中。

### 8.2 任务建立

`createTask()` 将所选类目与榜单做笛卡尔积：

```text
2 个类目 × 3 个榜单 = 6 个串行组合
```

品牌类型对整个任务固定。`rankLimit` 强制写为 200，周期策略强制写为“实时优先，否则近1天”。

### 8.3 单组合采集

每个组合按以下顺序执行：

1. 检查暂停或停止状态。
2. 选择榜单并校验 `aria-selected=true`。
3. 选择完整行业类目并校验选择结果；榜单切换重置类目时重新设置目标完整路径。
4. 选择品牌类型。
5. 选择实时或近1天并校验 radio 状态。
6. 返回第1页。
7. 等待表格稳定并识别商品列。
8. 逐行计算原始排名和趋势。
9. 仅对 up/first 获取业务字段和商品链接。
10. 组合内去重后加入结果。
11. 保存当前页检查点。
12. 翻页并同时验证页码增加和页面指纹变化。
13. 达到原始第200名、页面结束或平台实际数量不足时结束。

### 8.4 去重

组合内优先按规范化商品链接去重。规范化时保留 URL 的 origin、pathname 和商品 `id`，移除来源等无关参数。没有链接时使用“商品名称 + 空字符 + 店铺名称”。

同一商品出现在不同类目或不同榜单时必须保留多条，因为其排名、趋势和上下文不同。

---

## 9. 页面稳定、卡死与有限重试

### 9.1 稳定表格

`waitForStableTable()` 先固定等待，然后最多检查四轮；连续两次页面指纹一致且行数非零才视为稳定。无行时抛错，不用旧数据补足。

### 9.2 翻页验证

`advancePage()` 最多尝试三次。成功必须同时满足：

```text
新页码 > 旧页码
并且
新页面指纹 != 旧页面指纹
```

只变化其中一个不算成功，避免 React 旧 DOM 造成伪翻页和重复采集。

### 9.3 整组重试

页面长时间采集可能卡死。每个“类目 × 榜单”组合最多尝试三次：

1. 第一次失败后等待 1.5 秒，从筛选条件和第1页重新开始。
2. 第二次失败后等待 3 秒，再从第1页开始。
3. 第三次仍失败则把组合标记为 `failed`，记录页码、阶段和错误，继续后续组合。

失败尝试产生的局部结果不会提前加入正式结果；尝试期间产生的局部失败记录会在重试前清理，避免重复错误污染报告。

### 9.4 不允许的恢复方式

- 不刷新正在采集的 TOP200 页面。
- 不无限循环重试。
- 不高频连续点击。
- 不使用旧页或上一类目数据填空。
- 不并发操作多个组合。

---

## 10. 任务状态、检查点与恢复

### 10.1 任务状态

可能的任务状态：

- `pending`：已创建未运行。
- `running`：正在运行。
- `stopped`：用户要求停止，可续跑。
- `failed`：任务级异常，例如无法建立浏览器连接，可从任务文件续跑。
- `completed`：队列已处理完；其中仍可能存在单个失败组合，必须同时检查 `failures` 和组合状态。

组合状态为 `pending`、`running`、`completed` 或 `failed`。

### 10.2 保存位置

- `data/tasks/<taskId>.json`：任务完整状态，始终保留。
- `data/checkpoints/<taskId>.json`：未完成任务检查点。
- `reports/`：任务完成后生成的报告。

JSON 通过“写临时文件，再 rename”原子更新，降低意外关机导致半个 JSON 的风险。

### 10.3 暂停、停止与续跑

- 暂停：在安全检查点轮询等待，每 400ms 检查一次停止状态。
- 继续：活动任务直接解除暂停；没有活动运行器时重新启动任务。
- 停止：设置停止标志，采集循环在下一安全检查点抛出 `StopRequestedError`。
- 续跑：已经完成的组合跳过；未完成组合从该组合第1页重新执行，避免页内半成品重复。
- 重试失败：只重新运行 `failed` 组合，并先移除这些组合的旧结果和旧失败记录。

---

## 11. 数据结构与字段参考

### 11.1 任务级结构

核心字段：

| 字段 | 含义 |
|---|---|
| `taskId` | `top200-时间戳-4位随机串` |
| `taskName` | 面向普通用户的可读任务名称，用于历史任务和结果下拉框展示；旧任务由服务端兼容生成 |
| `parameters` | 类目、品牌、榜单、周期策略、`rankLimit=200` |
| `stats` | 组合、扫描、up、first、排除、解析异常、重复统计 |
| `current` | 当前组合、页码、当前组合扫描数和重试次数 |
| `combinations` | 每个类目 × 榜单的状态与统计 |
| `results` | 只含 up/first 的正式结果 |
| `failures` | 趋势、商品链接或组合级失败 |
| `reportFiles` | 完成后生成的 JSON/XLSX 绝对路径 |

### 11.2 结果明细字段

| JSON 字段 | Excel 列 | 说明 |
|---|---|---|
| `taskId` | —（仅 JSON） | 本次任务ID |
| `collectedAt` | 顶部“采集时间” | JSON 保留 ISO 时间；Excel 固定显示北京时间易读文本 |
| `categoryPath` | 行业类目（多类目时） | 完整路径 |
| `brandType` | 顶部“筛选” | 三选一 |
| `rankingType` | 工作表名与顶部“筛选” | 当前榜单 |
| `requestedPeriod` | —（仅 JSON） | 固定期望实时 |
| `actualPeriod` | —（仅 JSON） | 实时或近1天 |
| `rank` | 当前排名 | 平台原始 TOP200 名次 |
| `trendType` | —（仅 JSON） | `up` 或 `first` |
| `trendText` | 趋势原文 | `↑ N` 或首次上榜 |
| `riseCount` | 上升名次 | 首次上榜为空 |
| `productName` | 商品名称 | 页面文字 |
| `priceRange` | 价格/价格带 | 页面原文 |
| `productUrl` | 商品链接 | Excel 中为可点击超链接 |
| `shopName` | 店铺名称 | 不含二维码或店铺链接 |
| `userPaymentAmount` | 用户支付金额 | 页面区间原文 |
| `clickCount` | 点击次数 | 页面区间原文 |
| `clickConversionRate` | 点击成交转化率 | 页面百分比区间原文 |
| `orderCount` | 商品成交件数 | 页面区间原文 |
| `page` | 页面号 | 来源页 |
| `rowIndex` | 页内序号 | 1开始 |
| `extraFields` | 动态列 | 不同榜单的其他可见业务列 |
| `combinationId` | 不单独显示 | 类目路径与榜单组成的组合键 |

固定映射以外的可见业务列进入 `extraFields`。排名列、“操作”列、直播账号、带货账号和带货短视频不进入动态字段；组合对象的 `headers` 保存当次原始表头快照。

### 11.3 失败记录

失败记录包含：组合ID、类目、榜单、实际周期、页码、阶段、错误原因、重试次数和记录时间。

常见阶段：

- `趋势解析`
- `商品链接`
- `组合采集`

---

## 12. Excel 与 JSON 输出

### 12.1 文件命名

```text
reports/TOP200-YYYYMMDD-HHMMSS-<taskId>.json
reports/TOP200-YYYYMMDD-HHMMSS-<taskId>.xlsx
```

历史报告不会被覆盖。

### 12.2 Excel 工作表

1. `任务汇总`：每个组合一行，包含类目、榜单、状态和扫描统计。
2. 每个已选榜单独立明细表，例如 `总榜明细`、`搜索榜明细`；顶部四行是采集时间、品类、筛选和统计，第 6 行开始为明细。采集时间固定显示为 `YYYY-MM-DD HH:mm:ss（北京时间）`，不依赖电脑系统时区。
3. `失败记录`：失败阶段、原因和重试次数。

汇总和失败表冻结首行，榜单明细冻结前六行；各表加粗表头并启用筛选。商品链接单元格使用 ExcelJS hyperlink 对象，显示蓝色下划线并可直接点击。Excel 不写入任务编号、实际周期、趋势类型，也不存在“店铺链接”列；这些技术字段仍保留在 JSON 中。

### 12.3 一致性要求

- JSON `results.length` 应等于所有榜单 Excel 明细行数之和。
- 正常无重复情况下，`results.length = stats.up + stats.first`。
- Excel 当前排名必须在 1..200 内，并保留原始空缺，不重新编号。
- 失败记录不应被静默丢弃。

---

## 13. 本地 HTTP API 参考

服务地址默认是 `http://127.0.0.1:8080`。请求体上限为 1MB。

| 方法 | 路径 | 作用 |
|---|---|---|
| `POST` | `/api/discover` | 启动/连接 Edge，返回类目、榜单、品牌；可能返回 `needPath` 或 `needLogin` |
| `POST` | `/api/edge-path` | 验证并保存 `msedge.exe` 路径 |
| `GET` | `/api/status` | 当前活动任务、暂停状态、CDP状态、是否已发现选项 |
| `GET` | `/api/session` | 获取当前本地页面会话令牌，前端自动用于写请求 |
| `GET` | `/api/tasks` | 历史任务摘要列表，不携带完整结果与失败明细 |
| `POST` | `/api/tasks` | 校验参数、创建并启动任务 |
| `GET` | `/api/tasks/:taskId` | 获取单个任务 |
| `POST` | `/api/tasks/:taskId/pause` | 暂停活动任务 |
| `POST` | `/api/tasks/:taskId/resume` | 继续暂停任务或从任务文件续跑 |
| `POST` | `/api/tasks/:taskId/stop` | 请求停止活动任务 |
| `POST` | `/api/tasks/:taskId/retry` | 只重跑失败组合 |
| `GET` | `/api/tasks/:taskId/download?type=xlsx|json` | 下载报告，只允许读取 `reports` 内文件 |
| `GET` | `/api/events` | Server-Sent Events 日志和状态流，25秒心跳 |

创建任务请求示例：

```json
{
  "categories": [
    {
      "displayPath": "一级类目 / 二级类目",
      "segments": ["一级类目", "二级类目"]
    }
  ],
  "brandType": "不限",
  "rankingTypes": ["总榜", "搜索榜"]
}
```

服务端只接受本次 `/api/discover` 实际返回过的类目、品牌和榜单，避免前端伪造不存在的筛选项。

---

## 14. 网页功能

`public/index.html` 是不依赖外部资源的单文件页面，包含：

- Edge连接状态和类目/榜单发现。
- 类目搜索与多选。
- 品牌单选。
- 前六榜多选和预计周期提示。
- 任务开始、暂停、继续和停止。
- 组合进度、扫描数、上升数、首次上榜数。
- SSE 实时日志。
- 历史任务、续跑、重试失败、Excel/JSON下载。
- 按任务、类目、榜单和趋势筛选结果。
- 商品名称链接到真实商品地址；店铺只显示名称。

页面每2秒拉取一次状态和历史任务；SSE状态事件会触发额外刷新。

### 14.1 历史任务名称可读性改进（已实施）

当前页面显示可读 `taskName`，内部 `taskId`（例如 `top200-20260805081737-bv74`）保留在悬停提示、任务 JSON、检查点、报告文件名、API 路径和技术排错中。

新任务持久化 `taskName`，示例：

```text
2026-08-05 16:17｜总榜、搜索榜｜1个类目
```

生成规则为“创建日期时间 + 榜单名称 + 类目数量”，必要时追加品牌类型；同一分钟的重复任务追加序号。历史任务表和结果明细下拉框显示 `taskName`，鼠标悬停保留完整 `taskId`。旧任务没有该字段时，服务端根据已有 `createdAt`、榜单和类目数量生成兼容显示名称。

这项改动已实施，只改善识别和回顾，不改变任务唯一性、文件路径、API 参数、续跑逻辑或 JSON 内部关联。

### 14.2 历史任务数量控制（已实施）

`/api/tasks` 和网页采用“最近 10 个已完成任务 + 全部未完成任务”的显示策略：

- `completed`：主历史区最多显示最近 10 个。
- `pending`、`running`、`stopped`、`failed`：放入单独的“待处理任务”区域，不受 10 条限制，保留续跑和重试入口。
- `stopped` 代表已保存检查点、可以继续的任务，不能按无效记录自动隐藏或删除。
- 旧的已完成任务先只从主页面隐藏，不删除 `data/tasks`、报告或 JSON；后续可增加“查看全部历史”、搜索或用户主动归档/删除。

这项方案已在接口和页面实施，把“页面展示数量”和“磁盘数据保留”分开，避免页面越来越长，也避免误删可恢复任务。

#### 手动删除不再需要的历史记录

历史任务页面中的每条记录对应以下文件：

```text
data/tasks/<taskId>.json
```

只想让某条记录从“待处理任务”“最近历史”和结果任务下拉框中消失时：

1. 先停止对应任务并关闭采集服务的黑色窗口，避免运行中的程序重新写回任务文件。
2. 在网页任务名称上悬停，记下完整 `taskId`。
3. 打开项目内的 `data/tasks` 文件夹。
4. 只删除与该编号完全同名的 `<taskId>.json`。
5. 重新启动服务或刷新页面，记录即不再显示。

不要直接删除整个 `data` 目录，也不要按时间模糊批量删除。这里只删除页面记录，不会自动删除：

- `data/checkpoints/<taskId>.json` 检查点；
- `reports/` 中已经生成的 Excel 和 JSON 报告；
- 其他任务记录或项目代码。

如果这些关联文件以后也确实不需要，应再次核对相同 `taskId` 后分别处理，不能把“删除历史记录”理解为自动清理全部关联数据。

### 14.3 榜单专属表头与分表导出（已实施）

真实页面的六个业务榜单使用不同表头。总榜展示综合商品指标；搜索榜增加热搜词和商品曝光人数；直播榜增加直播间指标；商品卡榜使用商品卡口径；达人带货榜增加达人相关指标；短视频榜增加短视频相关指标。页面上的直播账号、带货账号和带货短视频只用于识别页面行，不写入结果数据。它们不能安全地共用一套固定明细列。

#### 设计目标

保留一个采集脚本，通过当前 `rankingType` 选择字段映射配置；采集结果按榜单分组，Excel 每个榜单单独一个明细工作表。流程如下：

```text
当前榜单
  -> 当前榜单真实 headers
  -> rankingType 专属字段映射
  -> 公共字段 + boardFields
  -> 按榜单分组
  -> 独立工作表
```

#### 榜单字段边界

| 榜单 | 专属字段示例 |
|---|---|
| 总榜 | 用户支付金额、点击次数、成交件数、点击成交转化率 |
| 搜索榜 | 热搜词、商品曝光人数、搜索用户支付金额 |
| 直播榜 | 直播间用户支付金额、直播间点击次数、直播间成交件数、带货直播间数；直播账号不记录 |
| 商品卡榜 | 用户支付金额、点击次数、点击成交转化率、商品成交件数 |
| 达人带货榜 | 用户支付金额、点击次数、达人成交件数、带货达人数；带货账号不记录 |
| 短视频榜 | 短视频用户支付金额、短视频点击次数、短视频成交件数、带货视频数；带货短视频不记录 |

同名或相似字段必须保留榜单语义；例如“直播间用户支付金额”不能覆盖或伪装成普通“用户支付金额”。

#### 推荐 JSON 形态

公共字段继续直接放在结果记录中；当前榜单专属字段放入 `boardFields`（实现时也可兼容现有 `extraFields`），并保留当前组合读取到的 `headers` 快照。直播账号、带货账号和带货短视频即使出现在原始表头中，也只作为页面定位信息，不进入 `boardFields`、网页结果或 Excel。这样既方便网页展示和 Excel 分表，也能在页面改版后回溯字段来源。

#### 推荐工作表

```text
任务汇总
总榜明细
搜索榜明细
直播榜明细
商品卡榜明细
达人带货榜明细
短视频榜明细
失败记录
```

每张明细表只包含公共字段和本榜单字段；多类目同榜单数据保留“行业类目”列并按行追加，不跨榜单拼接。字段映射按“`rankingType` + 表头语义 + 明确别名”执行，不能只按列序号执行。无法识别的业务表头进入该榜单动态字段；操作列、二维码入口等交互元素不进入业务数据；必需字段缺失则写入结构化失败记录。

#### 实施前回归验收

- 六个榜单可以由同一个采集脚本连续处理。
- 每个榜单生成独立明细表，表头不混合、不错位。
- 搜索榜热搜词和曝光人数、直播/达人/短视频业务指标保留在各自榜单；直播账号、带货账号和带货短视频不输出。
- JSON、网页结果和对应榜单 Excel 明细数量一致。
- 页面表头改名或新增字段时，有原始表头快照或失败记录可追踪。

本节目标架构已实施：继续使用一个采集脚本，榜单字段保存在各自 `extraFields`，组合保留原始 `headers` 快照，Excel 按榜单分表。

---

## 15. 新电脑安装与日常运行

### 15.1 首次安装

1. 安装 Node.js 20+ 与 Microsoft Edge。
2. 整体复制本项目目录，不复制或迁移 Edge Cookie。
3. 双击 `安装依赖.bat`。
4. 看到 `Installation completed` 即完成。

命令行等价操作：

```powershell
cd 'D:\前端\ai\Script All\Tiktok TOP200'
npm install
```

### 15.2 日常运行

1. 双击 `启动采集.bat`。
2. 保持黑色窗口打开。
3. 默认浏览器会打开 `http://127.0.0.1:8080`。
4. 点击“读取 / 刷新类目和榜单”。
5. 在程序弹出的调试版 Edge 商机中心入口中登录。
6. 回到本地网页，点击“我已登录，自动进入 TOP200”。
7. 选择类目、品牌和榜单，开始采集。
8. 完成后在历史任务下载 Excel 或 JSON。

### 15.3 停止服务

关闭黑色服务窗口即可停止本地服务。不要在任务运行中直接结束进程；优先点击网页“停止”，等任务状态保存后再关闭窗口。

---

## 16. 测试与验收记录

### 16.1 自动测试

运行：

```powershell
npm run check
```

当前共 27 项测试，覆盖：

- 一位和多位上升名次。
- 首次上榜。
- 下降、持平、横线、空趋势和无法识别趋势。
- 实时优先与近1天回退。
- 商品链接规范化与组合内去重。
- 固定字段和动态字段映射。
- 页码与商品指纹双重翻页验证。
- 页面卡顿第三次恢复和三次耗尽。
- Aurora/Ecom 两套 DOM fixture。
- 正确的商机中心登录入口与 TOP200目标地址分离。
- 检查点恢复。
- Excel、JSON和统计数量一致。
- Windows批处理保持纯ASCII。
- 搜索榜纯排名、独立趋势容器和二维码排除。
- 榜单字段隔离、分表导出与 Excel 禁止字段。
- 可读任务名称、旧任务兼容与最近 10 个已完成任务策略。
- 本机 Host/Origin 边界、写请求令牌和历史摘要接口。
- 末页排名、空表头列对齐和商品 URL 协议白名单。
- 榜单切换发生在目标类目设置与校验之前。
- Excel 榜单明细、任务汇总使用固定北京时间展示，JSON 原始 ISO 时间保持不变。

### 16.2 真实页面验收

2026-08-06 修复后完成搜索榜近 1 天第 1 页烟雾测试：读取 10 行，识别上升 5、下降 2、持平 1、无趋势 2、无法解析 0；真实标题类名为 `infoName-* / productTitle-*`，标题单击成功取得商品详情 URL，二维码未参与链接获取。

此前 2026-08-05 的完整 TOP200 基线验收如下：

2026-08-05 使用真实调试版 Edge 完成一次完整原始 TOP200：

| 指标 | 结果 |
|---|---:|
| 页面 | 20 |
| 扫描 | 200 |
| 上升 | 26 |
| 首次上榜 | 128 |
| 排除 | 46 |
| 保存明细 | 154 |
| 失败 | 0 |

对生成的真实 Excel 反向读取验证：

- 三张工作表名称正确。
- 154 条明细全部有商品链接。
- 154 个商品链接全部是可点击 hyperlink。
- 不存在店铺链接列或 `shopUrl` 字段。
- 154 个当前排名全部位于 1..200，互不重复，最大值为200。
- 排名没有被重新编号为1..154。

验收报告位于 `reports/`，任务ID为 `top200-20260805074258-fa2q`。

---

## 17. 已修复故障记录

### 17.1 双击批处理无法启动

**现象**

Windows 双击或执行 `启动采集.bat` 后出现：

```text
'exist' is not recognized as an internal or external command
```

窗口随后退出或停在 `Press any key to continue`。

**根因**

原批处理包含 UTF-8 中文提示，并把中文放在 `if (...)` 语句块中。当前 Windows `cmd.exe` 对该文件的 UTF-8 字节解析异常，导致原本正确的 `if not exist` 被拆坏。Node.js、依赖和 `server.js` 本身没有故障。

**修复**

- `启动采集.bat` 与 `安装依赖.bat` 改为纯 ASCII 内容。
- 不在批处理语句块中放中文或其他非 ASCII 字符。
- 新增 `test/windows-entrypoints.test.js`，逐字节验证两个入口全部 `< 128`。

**验证**

- `cmd.exe /d /c "启动采集.bat"` 成功输出本地地址并启动服务。
- `安装依赖.bat` 实际执行 `npm install` 成功。
- 以后修改这两个 `.bat` 时必须保持纯 ASCII；中文说明写在 Markdown 或网页中。

### 17.2 登录抖店地址错误

**现象**

程序启动调试版 Edge 后直接打开罗盘 TOP200 地址，用户看到的登录入口与“商机中心”程序不一致。

**根因**

早期 `edge.js` 把 `TARGET_URL` 同时当成 Edge 首开地址。业务目标页和用户登录入口被混为一个地址。

**修复**

```js
const LOGIN_URL = 'https://fxg.jinritemai.com/ffa/bu/NewBusinessCenter';
const TARGET_URL = 'https://compass.jinritemai.com/shop/chance/merchandise-product-rank';
```

- Edge 首开 `LOGIN_URL`，与同级商机中心程序完全一致。
- 用户登录确认后，`collector.js` 自动进入 `TARGET_URL`。
- 如果目标页仍未登录，程序主动返回商机中心入口并提示重新登录。
- 网页登录弹窗和使用说明已同步更新。
- 新增 `test/edge-config.test.js` 锁定两个地址必须分离。

**重启要求**

修改 `edge.js` 后，已经运行的 Node 进程不会自动加载新代码。验证时应关闭旧黑色服务窗口和程序弹出的调试版 Edge，再重新双击 `启动采集.bat`。

---

## 18. 故障排查

### 18.1 本地网页没有打开

1. 查看黑色窗口是否显示 `TOP200 采集服务已启动：http://127.0.0.1:8080`。
2. 手动访问 `http://127.0.0.1:8080`。
3. 检查8080是否被其他本地服务占用。
4. 命令行运行 `node server.js` 阅读完整错误。

### 18.2 提示未安装依赖

运行：

```powershell
npm install
```

确认 `node_modules/playwright-core` 存在。不要复制其他项目的 `node_modules`。

### 18.3 找不到 Edge

在网页填写 `msedge.exe` 完整路径。常见位置：

```text
C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
C:\Program Files\Microsoft\Edge\Application\msedge.exe
```

路径会保存在 `.edge-path.txt`。

### 18.4 一直提示登录

1. 确认登录发生在程序启动的调试版 Edge，不是日常默认 Edge。
2. 确认页面地址先是商机中心 `fxg.jinritemai.com/ffa/bu/NewBusinessCenter`。
3. 完成平台验证后回本地网页点击“我已登录，自动进入 TOP200”。
4. 不要复制 Cookie，不要手工改 `user-data-dir`。

### 18.5 9222连接失败

1. 关闭程序弹出的旧调试 Edge。
2. 重新点击发现，让程序重启带 `--remote-debugging-port=9222` 的 Edge。
3. 浏览器中访问 `http://127.0.0.1:9222/json/version` 检查是否返回 JSON。
4. 检查是否有其他程序占用9222。

### 18.6 页面卡住或翻页失败

- 程序会自动有限重试，先等待。
- 不要刷新或连续点击 TOP200 页面。
- 三次仍失败会记录组合失败并继续后续组合。
- 网络恢复后，在历史任务点击“重试失败”。

### 18.7 商品链接为空

检查失败记录是否出现“商品链接”。优先检查商品标题是否仍有 `href` 或能打开新页，以及平台是否把标题行为改为同页导航；二维码不参与排查。修改前先保存脱敏 DOM 和真实行为证据。

### 18.8 发现类目或榜单失败

可能原因：登录失效、账号权限不同、页面改版或 DOM 前缀变化。先检查页面真实 DOM，不要直接增加模糊选择器或绕过验证。

---

## 19. 已知限制和风险

1. 抖店页面随时可能改版，类名、层级、标签数量和链接交互都可能变化。
2. 不同账号权限可能看到不同类目、榜单和业务指标。
3. 采集多个组合耗时较长；每条保留商品还要打开一次链接页。
4. 平台金额、点击量和成交件数通常是区间文本，程序不会擅自转换为精确数。
5. 单个组合失败后，整体任务最终仍可能是 `completed`；判断完整性必须看组合状态和失败记录。
6. 服务重启会丢失内存中的 `discoveryCache`，但不会丢失任务JSON；创建新任务前重新发现即可。
7. 当前结果只按组合内去重，不做跨类目或跨榜单合并。
8. 商品链接优先依赖标题 `href`，无 `href` 时依赖标题点击后出现新页面的当前交互。
9. `logs/` 目录目前没有持久化运行日志；完整状态以任务JSON、失败记录和网页实时日志为准。

---

## 20. 后续修改原则

### 20.1 修改页面选择器前

1. 在真实调试版 Edge 中复现。
2. 保存脱敏 DOM fixture 或截图证据。
3. 确认 Aurora、Ecom 或新的组件前缀。
4. 先写或更新失败测试。
5. 做最小选择器修改。
6. 运行 `npm run check`。
7. 至少做一页真实页面 smoke test；涉及分页时做跨页验证。

### 20.2 不得擅自改变的口径

- 原始 TOP200 后过滤。
- 只保留上升与首次上榜。
- 实时优先，否则近1天。
- 前六榜，排除三个特殊榜。
- 品牌三选一。
- 商品链接必采，店铺只记名称。
- 当前排名保持平台原始名次。
- 不保存图片。
- 不迁移 Cookie。
- 有限重试，无限循环禁止。

### 20.3 依赖变更

- 保持 Node.js 20+。
- 使用 `playwright-core`，不要改为会自动下载 Chromium 的 `playwright`，除非用户重新确认。
- Excel库必须支持真正的 hyperlink 单元格。
- 变更依赖后运行 `npm audit --json` 与 `npm run check`。

---

## 21. 接手检查清单

下一位接手者开始工作前逐项确认：

- [ ] 已完整阅读本文档、环境约束和使用说明。
- [ ] 没有修改同级 `商机中心`。
- [ ] Node.js版本至少20。
- [ ] `npm install` 成功。
- [ ] `npm run check` 27项测试全部通过。
- [ ] 两个批处理仍为纯ASCII。
- [ ] `LOGIN_URL` 是商机中心地址。
- [ ] `TARGET_URL` 是罗盘TOP200地址。
- [ ] 调试Edge使用9222和独立用户目录。
- [ ] 用户在调试Edge中手动登录。
- [ ] 页面能发现类目和前六榜。
- [ ] 品牌是三选一。
- [ ] 实时/近1天规则正确。
- [ ] 翻页同时验证页码和指纹。
- [ ] 卡死整组最多尝试三次。
- [ ] Excel包含原始当前排名和可点击商品链接。
- [ ] Excel和JSON不包含店铺链接。
- [ ] 检查点、续跑和失败重试仍可用。
- [ ] 至少完成一次真实页面小范围验证。

---

## 22. 相关文档和参考

- [普通用户使用说明](./使用说明.md)
- [抖店环境与采集约束](./抖店环境与采集约束.md)
- [原始需求与技术基线](./技术交接文档-TOP200采集系统.md)
- 只读参考：`../商机中心/edge.js`
- 只读参考：`../商机中心/技术文档-商机中心采集系统.md`
- 只读参考：`../商机中心/交接提示词.md`
- 只读参考：`../商机中心/商机中心数据采集-踩坑记录.md`

---

## 23. 当前交付结论

项目当前具备从安装、登录、选项发现、组合采集、TOP200趋势过滤、商品链接抓取、有限重试、检查点恢复，到按榜单 Excel/JSON 导出的完整闭环。Windows批处理、登录入口、搜索榜趋势、商品标题链接、榜单字段隔离、可读任务名称和历史数量控制问题均已修复并加入回归测试。

后续接手者应优先维护页面兼容性和数据完整性，不要通过放松校验、刷新页面、增加点击频率或迁移Cookie换取表面成功。

## 24. 项目备份

- 备份目录：`D:\前端\ai\Script All\Tiktok TOP200-backups`
- 最近备份：`Tiktok TOP200-latest-20260807-143533.zip`
- 备份时间：2026-08-07 14:35（Asia/Shanghai）
