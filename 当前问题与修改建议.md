# TOP200 采集系统当前问题与修改建议

> 文档状态：已解决问题与待处理问题汇总
> 更新记录：2026-08-05 问题已于 2026-08-06 全部修复；2026-08-07 新增问题已完成处理确认。

> 说明：本文已合并历史问题记录。阅读或接手时，请以各章节的状态为准，不要将已解决问题误认为当前待修复问题。后续新增测试问题继续追加到本文档。

## 0. 当前问题状态总览

### 已解决：2026-08-05 记录的问题

2026-08-05 记录的搜索榜趋势解析、商品链接获取、Excel 按榜单拆分、任务名称、历史任务显示数量、榜单字段隔离及相关安全校验问题，已于 2026-08-06 完成生产代码修复，并通过 26 项自动测试及搜索榜第一页真实页面烟雾测试。

本文第 1 至第 9 节是已解决问题的历史调查证据、修复依据和验收记录，后续操作人员不应重复处理这些问题，除非回归测试再次发现问题。

### 已闭环：2026-08-07 记录的问题

第 10 节的 3 项现已全部闭环：

1. 类目搜索输入包含 `/` 时结果为空：用户确认是输入空格差异导致，无需修改程序。
2. 总榜修改行业类目后切换其他榜单，行业类目没有同步：已调整为先切换榜单，再设置并校验目标类目。
3. Excel 第一行采集时间显示为机器格式：已固定转换为北京时间易读格式。

当前没有文档中已知但尚未处理的代码问题。

## 0. 处理结论（2026-08-06）

- 搜索榜趋势只读取独立趋势容器；纯排名数字不再误报为无法解析。
- 商品链接优先读取标题 `href`，否则最多单击、双击标题各一次；二维码入口明确跳过。
- Excel 已按榜单拆分，顶部四行集中公共信息，并移除任务编号、实际周期、趋势类型；JSON 保持完整。
- 新任务持久化 `taskName`，旧任务由服务端兼容生成显示名称；页面保留 `taskId` 提示。
- 历史接口和页面显示最近 10 个已完成任务以及全部未完成任务，不删除磁盘数据。
- 榜单专属字段继续保存在各记录的 `extraFields` 中，组合保存原始 `headers`；直播账号、带货账号、带货短视频不进入结果。
- 服务只监听 `127.0.0.1`，校验 Host/Origin，写请求使用本机会话令牌；历史列表接口只返回摘要，选中任务后再读取完整结果。
- 末页排名按首个完整页面的稳定容量推导，空表头保留列占位；商品 URL 只接受 `http/https`。

真实页面烟雾测试（2026-08-06）：搜索榜近 1 天第 1 页读取 10 行，识别上升 5、下降 2、持平 1、无趋势 2、无法解析 0；实际标题类名 `infoName-* / productTitle-*` 可被定位，单击标题取得真实商品详情 URL，并关闭新页。

## 1. 用户确认的 Excel 调整

### 1.1 不再写入 Excel、只保留在 JSON/任务数据中的字段

- 任务编号
- 实际周期
- 趋势类型

这些字段继续保留在任务 JSON 和程序内部数据中，便于恢复、排错和二次处理。

### 1.2 Excel 按榜单拆分

组合选择多个榜单时，不再把所有明细混在一个“符合条件明细”工作表中。每个榜单生成独立工作表，例如：

- 总榜
- 搜索榜
- 直播榜
- 商品卡榜
- 达人带货榜
- 短视频榜

建议继续保留“任务汇总”和“失败记录”两个辅助工作表，用于检查完整性。

### 1.3 公共信息集中放在榜单工作表顶部

参考用户截图，每个榜单工作表顶部建议使用四行公共信息：

1. 采集时间
2. 品类
3. 筛选：品牌类型、榜单名称等
4. 采集统计：扫描、上升、首次上榜、排除、无法解析、保存和失败数量

第 6 行开始放商品明细表头。公共信息不再在每条商品明细中重复。

如果一个榜单同时选择多个类目，建议顶部列出全部类目，并在商品明细中保留“行业类目”列以避免丢失记录归属；只有单类目时可省略该明细列。

## 2. 搜索榜趋势解析问题

### 2.1 已有失败证据

任务 `top200-20260805081737-bv74` 的搜索榜数据：

- 扫描：200 条
- 保存：43 条
- 无法解析趋势：104 条
- 商品链接失败：43 条

任务最终状态虽然是 `completed`，但结果并不完整。

### 2.2 真实页面 DOM 证据

搜索榜的第一列同时包含原始排名和趋势。例如：

- `4`：排名 4，没有趋势
- `6 / 2`：排名 6，下降 2
- 第一至第三名使用奖牌图片，文字区域可能只显示趋势

趋势区域有独立容器，当前类名特征为 `changeRatioWrap-*`；升降值使用 `cp-change-ratio-value`，升降图标分别带 `upIdentify-*`、`downIdentify-*`。

### 2.3 根因

当前 `collector.js` 把整个第一单元格的文字作为趋势文本。当某行没有趋势时，原始排名数字（如 `4`、`5`）被送入 `parseTrend()`，最终被误判为 `unparseable`。

### 2.4 修改建议

1. 原始排名继续按现有排名规则读取或按页码推导。
2. 趋势文本只从 `changeRatioWrap-*` 趋势容器读取，不能读取整个排名单元格。
3. 没有趋势容器、没有首次上榜标识时按 `none` 处理，而不是把排名数字当成趋势。
4. 保留 `upIdentify-*`、`downIdentify-*`、`equal` 和“首次上榜”的结构化判断。

## 3. 搜索榜商品链接问题

### 3.1 真实页面证据

当前商品列中至少存在两种交互：

- 商品名称文字：用户确认单击或双击可进入商品页面。
- 右侧二维码图标：当前类名为 `ecom-popover-open`，点击后显示“扫码查看商品详情”。

二维码图片本身不是商品链接，不应采集、解析或写入报告。

### 3.2 根因

当前 `resolveProductUrl()` 固定点击商品单元格最后一个 SVG。在搜索榜中，最后一个 SVG 是二维码图标，因此不会打开商品页面；旧逻辑等待新页面超时后把链接记为空，并产生失败记录。

### 3.3 修改建议

1. 优先读取商品名称元素或其父元素上的真实 `href`。
2. 没有直接 `href` 时，点击商品名称文字并捕获新打开页面的 URL。
3. 单击没有触发时，只再尝试一次双击，保持有限重试。
4. 明确排除 `svg.ecom-popover-open` 等二维码入口，不点击、不解码、不保存二维码图片。
5. 页面只有二维码、没有真实商品链接时，保留商品业务记录，`productUrl` 为空，不把二维码当作链接。
6. 页面存在真实链接入口但两次尝试仍无法取得 URL 时，才写入“商品链接”失败记录。

## 4. 建议的测试顺序

修改前先新增以下失败测试：

1. 搜索榜排名单元格：只有排名数字时趋势为 `none`。
2. 排名与上升/下降数字同时存在时，只解析趋势容器。
3. 前三名奖牌图片不影响原始排名推导。
4. 商品标题有 `href` 时直接读取链接。
5. 商品标题通过单击或双击打开新页时能取得并关闭新页。
6. 二维码图标被明确跳过。
7. 多榜单任务生成多个同名榜单工作表，而不是一个混合明细表。
8. 榜单工作表顶部包含采集时间、品类、筛选和统计。
9. Excel 中不存在“任务编号、实际周期、趋势类型”，JSON 中仍保留。
10. JSON 结果数与所有榜单工作表明细数之和一致。

自动测试通过后，按以下顺序做真实页面验证：

1. 搜索榜近 1 天第 1 页烟雾测试。
2. 手工核对无趋势、上升、下降、持平各至少一条。
3. 手工验证商品名称单击入口和生成的商品 URL。
4. 再运行完整搜索榜原始 TOP200。
5. 最后运行“总榜 + 搜索榜”组合任务，检查两个独立工作表和失败记录。

## 5. 明日修改范围

建议只修改以下文件：

- `collector.js`：趋势容器读取、商品标题链接获取、跳过二维码。
- `export-excel.js`：按榜单拆分工作表、顶部公共信息、移除指定 Excel 字段。
- `test/`：增加上述回归测试和脱敏 DOM fixture。
- `使用说明.md`、`完整技术交接文档-TOP200采集系统.md`：测试通过后同步新输出结构和链接规则。

不要修改同级“商机中心”，不要迁移 Cookie，不要通过高频点击或无限重试绕过页面问题。

## 6. 当前代码状态

- 生产代码已按第 2 至第 5 节完成修改。
- 已新增趋势、商品链接候选、榜单字段隔离、Excel 分表、任务名称、旧任务兼容和历史数量控制测试。
- `npm run check` 当前通过 26 项测试；搜索榜第一页真实页面烟雾测试已通过，完整 TOP200 仍可按第 4 节继续复验。

## 7. 历史任务名称过长、普通用户难以识别（新增需求）

### 7.1 当前现象

网页“历史任务”和“结果明细”的任务下拉框直接显示内部任务编号，例如：

```text
top200-20260805081737-bv74
```

这个编号适合程序定位文件和接口请求，但不适合普通用户回顾“昨天采集的是什么”。当前任务编号由 `top200-时间戳-4位随机串` 组成，过长且不包含类目、榜单等可读信息。

### 7.2 建议方案：内部编号与用户显示名称分离

保留现有 `taskId`，新增一个持久化字段 `taskName`（或 `displayName`）专门用于网页展示：

```text
2026-08-05 16:17｜总榜、搜索榜｜1个类目
```

建议生成规则：

1. 日期和时间取任务创建时间，格式为 `YYYY-MM-DD HH:mm`，便于按天回顾。
2. 榜单显示为已选择的榜单名称；多个榜单用顿号连接，过多时显示“等 N 个榜单”。
3. 类目显示数量，例如“1个类目”“3个类目”；不把很长的完整类目路径全部塞进名称。
4. 品牌类型只有在确实需要区分时追加，例如 `｜非知名品牌`；默认“不限”可省略。
5. 如果同一分钟创建多个相同任务，在名称末尾追加“②”“③”等序号，避免用户无法区分。

界面建议调整为：

- 历史任务表第一列标题改为“任务名称”，显示 `taskName`。
- 结果明细任务下拉框显示 `taskName`。
- 鼠标悬停或详情区域保留完整 `taskId`，供技术排错和反馈问题时使用。
- 下载文件名、API 路径、任务 JSON、检查点和报告内部关联继续使用 `taskId`，不因改名破坏已有任务。

### 7.3 兼容旧任务

旧任务没有 `taskName` 时，前端可临时根据 `createdAt`、`parameters.rankingTypes` 和类目数量生成同样格式的显示名称；不修改旧任务文件也能正常展示。新建任务再把名称写入任务 JSON，保证服务重启后名称稳定。

### 7.4 方案边界

本条已实施：新任务写入 `taskName`，旧任务由服务端兼容生成；页面显示可读名称并以悬停提示保留 `taskId`。报告文件名和 API 路径仍使用 `taskId`。

## 8. 历史任务过多时的显示策略（新增需求）

### 8.1 当前问题

`listTasks()` 当前会读取 `data/tasks` 下的全部任务，网页也会把全部任务逐行显示。任务数量增加后，历史区域会越来越长，结果筛选下拉框也会变得难以使用。

### 8.2 推荐方案：最近 10 个已完成任务 + 未完成任务单独保留

不建议简单地只显示 10 条或自动删除旧文件，因为 `stopped`、`pending`、`running`、`failed` 任务仍可能需要“续跑”或“重试失败”。推荐把页面分成两类：

- **最近历史**：最多显示最近 10 个 `completed` 任务，按创建时间倒序。
- **待处理任务**：所有 `pending`、`running`、`stopped`、`failed` 任务单独显示，直到用户续跑、重试完成或明确归档。

截图中的 `stopped` 不是无效数据，而是已经保存检查点、可以继续采集的任务。它们可以不混在“最近历史”里，但不能因为数量限制而丢失续跑入口。

### 8.3 数据保留与页面显示分离

“只保留 10 个”建议先定义为“页面主列表只展示 10 个”，不要直接删除磁盘文件：

1. 旧的已完成任务 JSON、Excel 和检查点关联继续保留，避免误删历史报告。
2. 页面新增“查看全部历史”或按日期搜索入口，需要时再加载更早任务。
3. 对确实不需要的旧任务，另做用户主动确认的“归档/删除”操作，不自动清理。
4. 结果明细的任务下拉框默认只列最近 10 个已完成任务和全部待处理任务，避免下拉框无限增长。

### 8.4 验收要求

- 运行超过 10 次后，主历史区域仍不会超过 10 个已完成任务。
- 任意未完成任务仍可看到状态、续跑或重试入口。
- `stopped` 任务不会被误判为已完成，也不会静默删除检查点。
- 页面筛选隐藏旧任务时，任务 JSON 和已生成报告不受影响。
- API 和任务文件仍可按原 `taskId` 精确定位。

本条已实施：`/api/tasks` 返回最近 10 个已完成任务和全部未完成任务；旧任务文件与报告不删除。

## 9. 不同榜单表头不同，必须按榜单分别整理（新增需求）

### 9.1 截图确认的字段差异

截图覆盖当前页面的六个业务榜单。它们共享“排名、商品、趋势、商品链接”等基础概念，但中间业务列并不相同：

| 榜单 | 页面中特有或重点字段 | 不能直接复用的原因 |
|---|---|---|
| 总榜 | 用户支付金额、点击次数、成交件数、点击成交转化率 | 反映商品综合表现 |
| 搜索榜 | 热搜词、商品曝光人数、搜索用户支付金额 | 反映搜索词和搜索曝光，不是普通点击指标 |
| 直播榜 | 直播间用户支付金额、直播间点击次数、直播间成交件数、带货直播间数 | 直播账号只作为页面定位信息，不记录到结果 |
| 商品卡榜 | 用户支付金额、点击次数、点击成交转化率、商品成交件数 | 与总榜部分同名，但榜单口径不同 |
| 达人带货榜 | 用户支付金额、点击次数、达人成交件数、带货达人数 | 带货账号只作为页面展示信息，不记录到结果 |
| 短视频榜 | 短视频用户支付金额、短视频点击次数、短视频成交件数、带货视频数 | 带货短视频只作为页面展示信息，不记录到结果 |

因此，六个榜单的表头不能简单合并为一张固定表。强行合并会造成列名错位、指标含义错误，尤其是“用户支付金额”“成交件数”等同名字段在不同榜单中口径不同。

### 9.2 推荐技术方案：一个采集脚本，按榜单识别并分表输出

采集脚本仍然只保留一个，不为每个榜单复制一套程序。处理流程改为：

```text
读取当前榜单 rankingType
  -> 读取该榜单真实表头
  -> 按 rankingType 选择字段映射配置
  -> 解析公共字段 + 当前榜单专属字段
  -> 结果按 rankingType 分组
  -> Excel 每个榜单单独一个工作表
```

核心原则是“一个采集流程、六套字段定义、六张榜单明细表”。

### 9.3 数据结构建议

每条 JSON 结果保留以下三层信息：

1. **公共字段**：`rank`、`trendType`、`trendText`、`riseCount`、`productName`、`priceRange`、`productUrl`、`shopName`、`categoryPath`、`rankingType`、周期和采集时间。
2. **榜单专属字段**：按 `rankingType` 保存到 `boardFields`（也可沿用现有 `extraFields`，但必须带榜单上下文）。例如搜索榜可保存 `hotKeywords`、`productExposure`，直播榜保存直播间业务指标；直播账号、带货账号和带货短视频不保存。
3. **原始证据**：保存该组合当时读取到的 `headers` 或表头快照，便于页面改版后排查映射错误。

不要把“直播间用户支付金额”改名覆盖“用户支付金额”，也不要把“达人带货人数”塞进普通成交件数。字段显示名和业务语义必须保留。

### 9.4 Excel 输出建议

继续保留“任务汇总”和“失败记录”，但明细不再混在一个工作表中。建议工作表为：

- `总榜明细`
- `搜索榜明细`
- `直播榜明细`
- `商品卡榜明细`
- `达人带货榜明细`
- `短视频榜明细`

每张榜单表只放“公共字段 + 本榜单字段”，不显示其他榜单无关列。若同一榜单选择多个类目，保留“行业类目”列；同一榜单多次组合的数据按行追加，但不跨榜单混合。

### 9.5 字段映射规则

字段映射必须优先使用“榜单类型 + 表头语义”，不能只按第几列读取：

- 优先匹配当前榜单配置中的精确表头。
- 对页面少量文字变化允许维护明确的别名表，例如“店铺信息/店铺名称”。
- 未识别且非操作列的表头进入该榜单的动态字段，并写入原始表头快照。
- 必需字段缺失时记录结构化失败，不用其他榜单的同名列替代。
- “操作”列、二维码入口和纯交互列不作为业务字段保存。

### 9.6 验收要求

1. 同一个脚本可连续处理六个榜单，不需要复制六份采集代码。
2. 每个榜单都能生成独立明细工作表，不能出现混合表头。
3. 搜索榜的热搜词、曝光人数不会出现在总榜或商品卡榜列中。
4. 直播账号、带货账号和带货短视频不会写入 JSON、网页结果或 Excel；页面只用它们辅助识别当前榜单行。
5. 同名指标保留榜单语义，JSON 与 Excel 的字段数量和含义一致。
6. 页面新增或改名表头时，能在失败记录或原始表头快照中定位原因。
7. 所有榜单仍只处理平台原始 TOP200，趋势过滤和商品链接规则不变。

本条已实施：仍使用单一采集脚本，按榜单分表导出；各榜单动态字段隔离在各自 `extraFields` 与工作表中，并保存组合表头快照。历史报告不回写。

---

## 10. 2026-08-07 真实页面测试问题

### 10.1 行业类目搜索包含 `/` 时无法返回结果（用户确认无需修改）

#### 现象

行业类目搜索框可以正常搜索不包含斜杠的关键词，例如输入“生鲜”时能够显示相关类目；但输入“生鲜/”后，搜索结果为空。

截图证据：

- 输入“生鲜”可以显示类目结果：`C:\Users\23590\AppData\Local\Temp\codex-clipboard-2b2a4ae9-ed5a-4977-a5c3-992ae9568a60.png`
- 输入“生鲜/”没有任何结果：`C:\Users\23590\AppData\Local\Temp\codex-clipboard-60aac0ba-cdf1-4c6d-a32d-726909a241a4.png`

#### 复现步骤

1. 进入“配置任务”区域。
2. 在“搜索并多选行业类目”输入框中输入 `生鲜`。
3. 可以看到“生鲜 / 水果蔬菜”等完整类目路径。
4. 将搜索词改为 `生鲜/`。
5. 搜索结果为空。

#### 实际结果

搜索逻辑无法正确处理包含 `/` 的类目路径搜索词，导致用户无法直接通过带斜杠的完整路径片段查找类目。

#### 预期结果

搜索应支持类目完整路径中的 `/` 分隔符。输入 `生鲜/` 时，应至少返回以“生鲜 /”开头的类目，例如：

- 生鲜 / 水果蔬菜
- 生鲜 / 水果蔬菜 / 蔬菜 / 豆制品
- 生鲜 / 水果蔬菜 / 蔬菜 / 豆制品 / 百合

搜索不应因为 `/` 是路径分隔符而返回空结果，也不应破坏普通关键词搜索。

#### 处理结论（2026-08-07）

- 用户确认该现象来自输入值 `生鲜/` 与页面路径 `生鲜 /` 的空格差异，属于本次输入问题。
- 按用户要求不修改前端搜索逻辑，不增加相关测试，也不改变类目选择、保存或采集流程。

### 10.2 从总榜切换到其他榜单后，行业类目没有同步

#### 现象

在总榜中调整行业类目后，切换到其他榜单时，其他榜单页面没有保持总榜刚刚设置的行业类目，仍显示其他类目，进而可能导致后续读取或采集使用错误的行业类目。

截图证据：

- 总榜设置的行业类目示例：`C:\Users\23590\AppData\Local\Temp\codex-clipboard-79279f14-d17b-4def-9421-9ee794d7e5d2.png`
- 切换到搜索榜后行业类目未同步：`C:\Users\23590\AppData\Local\Temp\codex-clipboard-216539fa-5da0-4277-9dd1-693c78aa5a1d.png`
- 切换到直播榜后仍显示另一行业类目：`C:\Users\23590\AppData\Local\Temp\codex-clipboard-39ce0340-3420-4a5b-850d-7ad795a9c841.png`

#### 复现步骤

1. 进入总榜页面。
2. 调整行业类目，例如设置为某个具体类目路径。
3. 不重新修改行业类目，直接切换到搜索榜或直播榜等其他榜单。
4. 观察切换后的行业类目显示和后续读取结果。

#### 实际结果

- 总榜调整后的行业类目没有传递到其他榜单。
- 切换到搜索榜、直播榜等榜单后，页面可能继续显示之前的行业类目。
- 由于程序运行流程是先修改行业类目、再切换榜单，因此后续榜单读取可能使用错误的类目条件。

补充观察：其他榜单之间相互切换时，只要先修改过一次行业类目，之后切换榜单通常可以同步并正常运行；当前问题主要集中在“总榜调整类目后直接切换到其他榜单”的路径。

#### 预期结果

行业类目应作为任务组合的统一筛选条件。无论从总榜切换到其他榜单，还是在其他榜单之间切换，当前选定的行业类目都应保持一致。

每次榜单切换后，程序应确认页面实际显示的行业类目已经与当前组合要求一致，再继续读取榜单数据；不能仅依赖之前的操作状态或默认类目。

#### 后续修改建议

- 检查榜单切换流程与行业类目设置流程之间的状态同步，重点确认总榜是否使用了不同的页面组件、事件触发方式或筛选状态保存逻辑。
- 在切换榜单后增加行业类目状态校验：读取页面当前类目路径，与目标组合的完整类目路径比较。
- 如果校验不一致，应重新设置目标类目并等待页面稳定后再继续，不应直接读取表格。
- 对总榜 → 搜索榜、总榜 → 直播榜，以及搜索榜 → 直播榜等方向分别增加回归测试。
- 测试应验证页面显示类目、实际采集组合中的 `categoryPath` 和最终报告中的类目三者一致。
- 本问题不应通过高频点击、刷新采集页面或绕过页面状态校验解决；仍需遵守项目既有的固定等待、DOM 验证和有限重试规则。

#### 处理结果（2026-08-07）

- `collectCombination()` 的筛选顺序已改为“切换榜单 → 设置并校验目标行业类目 → 设置品牌类型 → 设置周期”。
- 榜单切换可能重置页面筛选时，后续 `selectCategory()` 会读取页面实际类目；不一致则重新设置，并沿用既有完整路径校验。
- 新增组合流程回归测试，锁定榜单切换必须发生在类目设置与校验之前。
- 真实调试版 Edge 已完成“总榜 → 搜索榜 → 目标类目校验”低频烟雾测试，最终页面类目与目标一致。

### 10.3 Excel 第一行采集时间显示为机器格式，普通用户难以理解

#### 现象

Excel 榜单明细表第一行的“采集时间”显示为机器可读的时间格式，普通用户无法直观看出这是北京时间，也不便于日常查看和记录。

#### 实际结果

采集时间以技术性时间字符串写入 Excel，用户看到的是机器专用格式，而不是明确的北京时间格式。

#### 预期结果

Excel 第一行的采集时间应转换为北京时间（Asia/Shanghai，UTC+8），并使用普通用户易读的格式，例如：

```text
采集时间：2026-08-07 14:30:25（北京时间）
```

具体格式可以保持统一的年月日、时分秒显示，但必须满足：

- 明确使用北京时间；
- 不显示难以理解的 ISO 技术格式或时区偏移字符串；
- 不因运行电脑的系统时区不同而改变业务含义；
- JSON 中用于程序处理的原始时间字段可以继续保留，不要求与 Excel 展示格式完全一致。

#### 后续修改建议

- 检查 Excel 榜单明细表顶部公共信息中“采集时间”的格式化逻辑。
- 导出展示时统一转换为 `Asia/Shanghai` 时区，并使用易读的中文或标准本地日期时间格式。
- 增加导出回归测试，确认 Excel 第一行显示北京时间，且跨系统时区不会产生偏移。
- 同时核对任务汇总表、榜单明细表和 JSON 中的时间字段，避免展示时间与任务实际采集时间不一致。

#### 处理结果（2026-08-07）

- Excel 榜单明细第一行统一显示 `YYYY-MM-DD HH:mm:ss（北京时间）`。
- 任务汇总的开始、结束时间和失败记录时间使用相同的北京时间展示格式；转换固定按 UTC+8 计算，不依赖运行电脑的系统时区。
- JSON 中的原始 ISO 时间字段保持不变。
- 导出回归测试已反向读取生成的 Excel，确认北京时间文本和 JSON 原始时间同时正确。

## 11. 合并文档当前处理范围

2026-08-07 最终处理范围为 `collector.js`、`export-excel.js` 和对应测试；类目搜索按用户确认不修改。`npm run check` 当前通过 27 项测试，并完成榜单切换后的类目真实页面烟雾验证。
